# Factory Ads — Strategy — <strategy name, e.g. "Price anchor">

> One file per reusable ANGLE, at `factory/ads/strategies/<strategy-id>.md`. A strategy is the
> argument an ad makes — independent of how the creative is produced.
>
> **Only create this file if the angle is genuinely reusable across more than one ad.** A one-off
> argument belongs in the ad's own file. Empty abstraction costs every future run a pointless read.

- **The argument:** <what this angle claims and why someone acts on it>
- **When it fits:** <the situations where this angle is the right one>
- **When it does NOT fit:** <where it falls flat or reads as dishonest>
- **Copy patterns that work:** <concrete phrasings, structures, lengths — accumulated, not theorized>
- **Claims discipline:** <what must be true before this angle can be used — a real price, a real
  feature, a real number. Never argue something the product can't back.>
- **Ads using this:** <links to `ads/` files, so the results roll up>
- **What the results have taught us:** <appended over time — this is the whole point of the file>
