# Factory Ads — Visual Method — <method name, e.g. "HTML/CSS template render">

> One file per reusable PRODUCTION TECHNIQUE, at `factory/ads/visuals/<visual-id>.md`. A visual is
> *how the creative gets made* — not a fixed template. The same method produces very different-looking
> ads; the ad file holds the look, this file holds the craft.
>
> **Only create this file if there's real know-how that outlives any single ad.** If the technique is
> "I wrote one image", keep it in the ad file.

- **What this method produces:** <still image | video | animation | plain text-on-color, and format>
- **Tooling:** <the skill, API, or library used, and the env var names for any credentials>
- **How to run it:** <the concrete invocation, where output lands, how long it takes>
- **Prompting / authoring notes:** <what actually works — accumulated. For generative tools, the
  style of prompt that gets good output vs. the style that wastes credits.>
- **Platform constraints:** <aspect ratios, durations, file sizes, text-in-image limits per platform>
- **Traps:** <everything that cost real time — silent failures, quality cliffs, expensive retries>
- **Ads using this:** <links to `ads/` files>
